import styled from "styled-components";

export const Container = styled.div`
    margin-top: 50px;

    .field {
        input[type=checkbox] {
            width: initial;
        }

        label {
            position: relative;
            bottom: 15px;
            font-size: 14px;
            margin-left: 10px;
        }
    } 
`;

